Algoritmo EvaluarFuncionY
    // Declaración de variables
    Definir x, y como Real

    // Para cada valor de x de 1 a 10
    Para x <- 1 Hasta 10 Con Paso 1
        // Calcular el valor de la función Y
        y <- (3 * x^2 + 12 * x + 6 * x + 9) / (2 * x^2 - 32)

        // Mostrar el resultado
        Mostrar "Cuando x =", x, ", Y =", y
    FinPara
FinAlgoritmo
